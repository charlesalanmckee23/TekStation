# Account Authentication v126

## UX update
- Successful account authentication now displays a prominent in-app signed-in state.
- Cloud-backup selection is handled inside the Data Vault rather than a browser confirm dialog.
- Users can choose Use cloud copy or Keep this device.

Existing D1 schema and account/session APIs remain unchanged.

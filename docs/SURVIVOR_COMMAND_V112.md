# TekStation v112 — Survivor Command Bar visual refresh

The Planner's Survivor Command Bar was visually refreshed to use the same button language as the rest of TekStation. The section now has a compact framed layout, uniform pill buttons, stronger hierarchy, and responsive tablet/mobile behavior. Existing ids and event hooks are preserved. Planner and implant scanning logic are unchanged.

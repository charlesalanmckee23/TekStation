v113 — Command Center markup cleanup

Removed literal \n escape sequences accidentally rendered as visible text around the Survivor Operations Command Center. Preserved all functionality and bumped the service worker cache.

{
  "name": "tekstation-ios",
  "version": "1.0.0",
  "private": true,
  "description": "TekStation ARK: Survival Ascended companion app for iOS, packaged with Capacitor.",
  "scripts": {
    "cap:add:ios": "npx cap add ios",
    "cap:sync": "npx cap sync ios",
    "cap:copy": "npx cap copy ios",
    "cap:open:ios": "npx cap open ios",
    "verify:web": "node scripts/verify-web.mjs"
  },
  "dependencies": {
    "@capacitor/core": "^8.0.0",
    "@capacitor/ios": "^8.0.0",
    "@capacitor/share": "^8.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.0.0"
  }
}
